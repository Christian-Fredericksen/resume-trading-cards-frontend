 class Card{
     constructor(id, img_src, full_name, bio){
         this.id = id;
         this.img_src = img_src;
         this.full_name = full_name;
         this.bio = bio;
     }


     // an instance method that is going to render the object to the DOM
 }